git clone https://github.com/ditt-användarnamn/kimposter.git
cd kimposter
# Här klistrar du in koden jag hjälpt dig bygga (eller jag kan ge dig som textfiler)
git add .
git commit -m "Första versionen av posterbutiken"
git push origin main
